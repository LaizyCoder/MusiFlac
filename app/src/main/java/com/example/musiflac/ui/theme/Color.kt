package com.laizycoder.musiflac.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * MusiFlac application palette.
 *
 * The dark palette is intentionally not pure black. It matches the soft
 * charcoal/plum background used by the Settings screen and is shared by
 * every screen through MusiFlacTheme.
 */

// Main application background.
val MusiFlacDarkBackground = Color(0xFF120F15)

// Cards, dialogs and elevated surfaces.
val MusiFlacDarkSurface = Color(0xFF1E1E1E)
val MusiFlacDarkSurfaceVariant = Color(0xFF242225)
val MusiFlacDarkSurfaceContainer = Color(0xFF202020)

// Primary text and icons.
val MusiFlacDarkOnBackground = Color(0xFFF5F1F7)
val MusiFlacDarkOnSurface = Color(0xFFF5F1F7)

// Secondary text.
val MusiFlacDarkOnSurfaceVariant = Color(0xFFA9A5AB)

// Dividers and subtle outlines.
val MusiFlacDarkOutline = Color(0xFF3A373C)

// Light theme kept centralized as well, so individual screens never need
// their own theme colors.
val MusiFlacLightBackground = Color(0xFFF8F6F9)
val MusiFlacLightSurface = Color(0xFFFFFFFF)
val MusiFlacLightSurfaceVariant = Color(0xFFF0EDF1)
val MusiFlacLightOnBackground = Color(0xFF1B181D)
val MusiFlacLightOnSurface = Color(0xFF1B181D)
val MusiFlacLightOnSurfaceVariant = Color(0xFF625D64)
val MusiFlacLightOutline = Color(0xFFD0CBD1)

// Accent colors.
val MusiFlacPrimary = Color(0xFFD0BCFF)
val MusiFlacPrimaryDark = Color(0xFF6650A4)
